feat: add PDF + ZIP export with company branding

PDF export (app/lib/atlas-document-pdf-export.ts):
- Full branded A4 PDF via jsPDF (Node.js server-side)
- Red brand header with company name + ICE
- Document metadata block
- Classification section with confidence pill
- Extracted fields tables (supplier + financial) with confidence dots
- Line items table
- Corrections log with amber highlights
- Validation status block
- Company identity footer block
- Multi-page support with automatic page breaks and footers

ZIP export (app/lib/atlas-document-export.ts):
- Bundles JSON + CSV + XML + XLSX into a single archive
- Includes README.txt with document metadata
- jszip installed, DEFLATE compression

Export API (app/api/documents/[id]/export/route.ts):
- Now supports: json, csv, xml, xlsx, pdf, zip (6 formats)
- PDF fetches company data for branding
- All formats log to atlas_entity_events + zafirix_exports

Documents IA action menu:
- Added: Telecharger PDF (real jsPDF)
- Added: Telecharger ZIP (all formats bundled)
