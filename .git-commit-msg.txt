feat: Documents IA accounting engine -- Phases 3-6

Implements the full Documents IA → Comptabilité → TVA workflow.

## Accounting Engine (Phase 5)
- New lib: atlas-documents-accounting-engine.ts
  - buildJournalLines(): Moroccan PCGE Debit/Credit entries
    * Purchase: Debit 61xxxx (charges HT) + Debit 34551 (TVA déductible) + Credit 44100 (fournisseur)
    * Sales:    Debit 34210 (client TTC)   + Credit 71100 (CA HT) + Credit 44550 (TVA collectée)
    * Account codes resolved from category/AI suggestion, fallback to 61200
  - persistJournalLines(): bulk insert to atlas_accounting_entries with source_document_id
  - buildTvaSuggestion(): per-invoice TVA draft with rate, period_key, base_ht, source linkage
  - persistTvaSuggestion(): inserts to zafirix_tva_suggestions

## TVA Engine (Phase 6)
- zafirix_tva_suggestions table: per-invoice TVA draft with status tracking
- TVA collectée/déductible auto-computed by existing computeTvaPeriod() from atlas_supplier_invoices
- New TVA suggestion persisted alongside supplier invoice for explicit audit trail

## Traceability (Phase 7)
- Every atlas_accounting_entries row now carries source_document_id + source_invoice_id
- Every zafirix_tva_suggestions row linked to source_document_id + source_invoice_id
- route-to creates: supplier_invoice → journal_lines → tva_suggestion atomically

## Route-to API rewrite
- Loads company TVA regime (mensuel/trimestriel) for correct period_key
- All accounting modules (comptabilite, supplier_invoices, fournisseurs, tva) → full flow
- Returns: { invoiceId, journalLineCount, journalEntryIds, tvaSuggestionId, tvaAmount }

## Validation Center (Phase 5 UI)
- Post-routing confirmation panel:
  * Green: Facture fournisseur créée (with link to /comptabilite)
  * Blue:  N écritures comptables créées (Debit/Credit)
  * Indigo: TVA déductible XXX MAD (with link to /tva)
- Routing suggestions hidden after first routing; "Envoyer vers autre module" toggle

## Migration: 20260601230000_documents_ia_accounting_engine.sql
- atlas_accounting_entries: + source_document_id, source_invoice_id, generated_by, validation_status
- zafirix_tva_suggestions table with RLS (select/insert own + service role insert)
